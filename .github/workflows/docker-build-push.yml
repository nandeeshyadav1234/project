# .github/workflows/docker-build-push.yml
name: Build & Push Docker to ECR

on:
  push:
    branches: [main]
    paths:
      - 'app1/**'
      - 'app2/**'
      - '.github/workflows/docker-build-push.yml'

env:
  AWS_REGION: us-east-1
  ECR_REGISTRY: 341354505137.dkr.ecr.us-east-1.amazonaws.com

jobs:
  docker:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: AKIAU66SQMOYSMJ4EUWF
          aws-secret-access-key: 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
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

          #  - name: Build & Push app1
          #  run: |
          #  docker build -t $ECR_REGISTRY/app1:latest /root/project/app1
          # docker push $ECR_REGISTRY/app1:latest

      - name: Build & Push app2
        run: |
          docker build -t $ECR_REGISTRY/app2:latest /root/project/app2
          docker push $ECR_REGISTRY/app2:latest

